---
description: Catch up — see what you've built and what's next
---

Catch this person up. They are probably lost, behind the room, or returning after a break.

Invoke the `marketing-team-lead` skill in catch-up mode. List `marketing-team/`, then answer in exactly three lines: what they've finished, what's next, how long it takes. Then start the next specialist without asking.

If the folder is empty and the session is already past the halfway point, run the rescue path in the skill's catch-up mode: write the demo positioning and white space files first, then start the Copywriter. Do not start the Copywriter against an empty folder.

$ARGUMENTS
